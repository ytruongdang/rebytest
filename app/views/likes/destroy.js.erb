<% if flash[:success] %>
  $('#<%= @supports.likeable.class.name.downcase + @supports.likeable.id.to_s %>')
    .html('<%= j render "likes/like", object: @supports.likeable %>');
  <% flash.delete :success %>
<% else %>
  alert('<%= flash[:danger] %>');
<% end %>
